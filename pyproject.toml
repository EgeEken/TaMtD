[project]
name = "TaMtD"
version = "0.1.0"
description = "Default template for PDM package"
authors = [
    {name = "EgeEken", email = "egeeken.tr@gmail.com"},
]
dependencies = ["transformers>=5.3.0", "torch>=2.7.1", "torchvision>=0.22.0", "pillow>=12.1.1", "datasets>=4.7.0", "matplotlib>=3.10.8", "mlflow>=3.10.1", "numpy>=2.4.3", "scikit-learn>=1.5.0", "pyyaml>=6.0", "opencv-python>=4.13.0.92", "decord2>=3.2.0"]
requires-python = "==3.12.*"
readme = "README.md"
license = {text = "MIT"}

[tool.pdm.scripts]
load-dataset = "python -m scripts.dataset_loader_example"

[tool.pdm]
distribution = false

[dependency-groups]
test = ["pytest>=8.0"]

[tool.pytest.ini_options]
pythonpath = ["src"]
